<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_marginVertical="1dp"
        android:padding="@dimen/card_item_padding">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp">

            <TextView
                android:id="@+id/textView_card_item_title"
                style="@style/TextAppearance.AppCompat.Title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/card_item_text_start_margin"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@{newsFeed.title}"
                app:layout_constraintBottom_toTopOf="@+id/textView_card_item_subtitle"
                app:layout_constraintEnd_toStartOf="@+id/imageView_card_item"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Title" />

            <TextView
                android:id="@+id/textView_card_item_subtitle"
                style="@style/TextAppearance.AppCompat.Small"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/card_item_text_start_margin"
                android:ellipsize="end"
                android:maxLines="4"
                android:text="@{newsFeed.subTitle}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/imageView_card_item"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/textView_card_item_title"
                tools:text="Subtitle" />

            <ImageView
                android:id="@+id/imageView_card_item"
                android:layout_width="80dp"
                android:layout_height="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:srcUrlNewsFeed="@{newsFeed.image}" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>

    <data>

        <import type="homework.chegg.com.chegghomework.ui.adapter.BindingAdapterKt" />

        <variable
            name="newsFeed"
            type="homework.chegg.com.chegghomework.data.modules.NewsFeed" />
    </data>

</layout>